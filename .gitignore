# Build output
build/

# Object files
*.o
*.a

# Editor files
*.swp
*.swo
*~
.vscode/

# Derived merge output (regenerated by scripts/merge_benchmarks.py).
# We commit hanya output/detail/* (per-mode mentah). File gabungan
# di output/result/ dihasilkan ulang lokal setelah pull, sehingga
# tidak ikut menyebabkan merge conflict pada git pull --rebase.
output/result/
# Stamp + log runtime FreeRADIUS yang di-generate prepare.sh / run_server.sh
output/freeradius_aaa/

